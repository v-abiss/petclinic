name: petclinic.Docker
on:
  workflow_dispatch:
  push:
    branches:
      - master
      - docker
  pull_request:
    branches: [ master ]
env:
  JAVA_VERSION: '1.8'  
jobs:
  CONTAINER_BUILD:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v2    
    
    - name: Login to the Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: ${{ secrets.ACR_LOGIN_SERVER }}
        username: ${{ secrets.REGISTRY_USERNAME }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
        
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: ${{ env.JAVA_VERSION }}
        
    - name: Build with Maven
      run: mvn package --file pom.xml
      
    - name: Install Trivy
      run: |
        #!/bin/bash
        sudo apt-get install wget apt-transport-https gnupg lsb-release
        wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
        echo deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main | sudo tee -a /etc/apt/sources.list.d/trivy.list
        sudo apt-get update
        sudo apt-get install trivy
        echo $HOME/Library/Caches
    
    - name: Build, Scan & Tag the Image
      run: |
        docker build -f ./Dockerfile -t ${{ secrets.ACR_LOGIN_SERVER }}/petclinic:${{ github.run_number }} .
        trivy --exit-code 0 --severity MEDIUM,HIGH ${{ secrets.ACR_LOGIN_SERVER }}/petclinic:${{ github.run_number }}
    
    - name: Push the Image
      run: |
        docker push ${{ secrets.ACR_LOGIN_SERVER }}/petclinic:${{ github.run_number }}
